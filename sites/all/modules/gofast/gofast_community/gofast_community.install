<?php


function gofast_community_install () {
  // Update the weight of the module to call it at the end
  // old school code query
  db_query("UPDATE {system} SET weight = 999 "
          . "WHERE type = 'module' AND name = 'gofast_community'");
}
